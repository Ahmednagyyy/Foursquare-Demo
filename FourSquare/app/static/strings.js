/**
 * This is for adding constant strings across the app
 */
/* istanbul ignore file */
import Config from 'react-native-config';

export const Strings = {
  // AppInfo
  VERSION_NUMBER: `V ${Config.VERSION_NUMBER}`,
  VERSION: `${Config.VERSION_NUMBER}`,
};
